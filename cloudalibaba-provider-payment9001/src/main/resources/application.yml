server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #配置Nacos的地址

#监控的端点（所有端点）
management:
  endpoints:
    web:
      exposure:
        include: '*'
